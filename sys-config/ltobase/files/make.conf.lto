# please set the following in the top of make.conf *before* using
#   OPT_FLAGS to your chosen optimization flags
#   NTHREADS to the desired number of parallel link threads

LTO="-flto=${NTHREADS}"
VEC="-ftree-vectorize -fvect-cost-model=cheap"
GRAPHITE="-fgraphite-identity -floop-nest-optimize"
TUNE="-fdevirtualize-at-ltrans"

COMMON_FLAGS="${OPT_FLAGS} ${LTO} ${VEC} ${GRAPHITE} ${TUNE} -fuse-linker-plugin"
